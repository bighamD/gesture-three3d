import { Panel, type PanelMode } from "./Panel.js";
import type { IUpdateArgs } from "./Interfaces/IUpdateArgs.js";
interface MemoryPerformance {
    usedJSHeapSize: number;
    jsHeapSizeLimit: number;
}
declare global {
    interface Performance {
        memory?: MemoryPerformance;
    }
}
export interface StatsOptions {
    glass?: boolean;
    defaultPanelMode?: PanelMode;
}
export declare class Stats {
    dom: HTMLElement;
    private _panels;
    private _frames;
    private _beginTime;
    private _mode;
    private _background;
    private readonly _glass;
    private readonly _defaultPanelMode;
    constructor(options?: StatsOptions);
    addPanel(name: string, foreground: string, msRefresh: number, refreshCallback: (time: number, delta: number) => IUpdateArgs | undefined): Panel;
    getPanel(name: string): Panel | undefined;
    showPanel(id: number): void;
    begin(): void;
    end(): number;
    update(): void;
    private _setupContainer;
}
export {};
