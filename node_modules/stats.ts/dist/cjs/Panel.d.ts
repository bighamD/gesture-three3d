import type { IUpdateArgs } from "./Interfaces/IUpdateArgs.js";
export interface Threshold {
    value: number;
    color: string;
}
export type PanelMode = "soft" | "hardcore";
export declare class Panel {
    private readonly _name;
    foreground: string;
    background: string;
    private readonly _msRefresh;
    private readonly _refreshCallback;
    private readonly _mode;
    readonly dom: HTMLCanvasElement;
    thresholds?: Threshold[];
    private readonly _canvas;
    private readonly _context;
    private readonly _data;
    private readonly _capacity;
    private readonly _graphX;
    private readonly _graphY;
    private readonly _graphWidth;
    private readonly _graphHeight;
    private readonly _width;
    private readonly _height;
    private readonly _pixelRatio;
    private readonly _text;
    private _index;
    private _length;
    private _min;
    private _max;
    private _lastTime;
    private _lastDisplayedValue;
    constructor(_name: string, foreground: string, background: string, _msRefresh: number, _refreshCallback: (time: number, delta: number) => IUpdateArgs | undefined, _mode?: PanelMode);
    get name(): string;
    get min(): number;
    get max(): number;
    init(): void;
    update(time: number): void;
    private _setupContext;
    private _drawTitle;
}
